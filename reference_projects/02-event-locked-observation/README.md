# Event-locked observation

Base-install reference for EEG, markers, identity processing, and an
event-locked window. Raw EEG and marker recording stays scoped to the observe
phase.

```bash
eegle compile .
eegle graph .
eegle rehearse .
eegle inspect .
eegle replay .
```
